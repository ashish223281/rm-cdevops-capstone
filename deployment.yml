apiVersion: apps/v1
kind: Deployment
metadata:
  name: rm-capstone
  labels:
    app: rm-capstone
spec:
  replicas: 3
  selector:
    matchLabels:
      app: rm-capstone
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: rm-capstone
    spec:
      containers:
      - name: rm-capstone
        imagePullPolicy: Always
        image: fmmrm/rm-capstone
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: rm-capstone
  labels:
    app: rm-capstone
spec:
  type: LoadBalancer
  selector:
    app: rm-capstone
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP

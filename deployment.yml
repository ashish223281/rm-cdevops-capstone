apiVersion: apps/v1
kind: Deployment
metadata:
  name: rm-capstone-project-cdevops
  labels:
    app: rm-capstone-project-cdevops
spec:
  replicas: 3
  selector:
    matchLabels:
      app: rm-capstone-project-cdevops
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: rm-capstone-project-cdevops
    spec:
      containers:
      - name: rm-capstone-project-cdevops
        imagePullPolicy: Always
        image: fmmrm/rm-capstone
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: rm-capstone-project-cdevops
  labels:
    app: rm-capstone-project-cdevops
spec:
  type: LoadBalancer
  selector:
    app: rm-capstone-project-cdevops
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
